/*
package utils

case class BoogieReturnResult(failures: List[Int], numSuccess: Int, numFailure: Int)
case object BoogieReturnResult {
  def apply(failures: List[Int], numSuccess: Int, numFailure: Int): BoogieReturnResult = {
    if (failures.size != numFailure) throw new Exception()
    new BoogieReturnResult(failures, numSuccess, numFailure)
  }
}
*/